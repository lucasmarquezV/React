import { View, Text } from 'react-native';
import { styles } from './styles';

const Projetos = () => {
  return (
    <View>
      <Text style={styles.titulo}>Projetos: </Text>
      <Text style={styles.texto}>MedGen</Text>
    </View>
  );
};

export default Projetos;
